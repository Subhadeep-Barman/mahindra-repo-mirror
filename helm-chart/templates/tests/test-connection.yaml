
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gto-projects.fullname" . }}-test-connection"
  labels:
    {{- include "gto-projects.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gto-projects.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
